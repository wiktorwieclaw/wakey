[target.xtensa-esp32-none-elf]
runner = "espflash flash --monitor"

[build]
target = "xtensa-esp32-none-elf"
rustflags = [
  "-C", "link-arg=-nostartfiles",
]

[env]
ESP_LOGLEVEL = "info"

[unstable]
build-std = ["core"]